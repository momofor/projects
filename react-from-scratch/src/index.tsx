import ReactDOM from "react-dom";
import * as React from "react";
import "./style.scss";
import App from "./app";
export {};

ReactDOM.render(
	<React.StrictMode>
		<App />
	</React.StrictMode>,
	document.getElementById("root")
);
